public class SimpleGoal : Goal
{
    public SimpleGoal(string goalType, string goalName, string goalDescription, int goalScore, bool complete) : base(goalType, goalName, goalDescription, goalScore)
    {
        // Additional initialization or custom logic for EternalGoal
    }
    bool _complete;
    
}
